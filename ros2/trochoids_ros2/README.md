# trochoids_ros2

ROS2 ament package exposing the trochoids C++ solver library for ROS2 workspaces.

## Build

```bash
cd ~/ros2_ws/src
git clone <repo-url>
cd ..
colcon build --packages-select trochoids_ros2
```
